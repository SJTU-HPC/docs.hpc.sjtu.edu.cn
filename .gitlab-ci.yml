stages:
  - build
  - deploy

mkdocs_build:
  stage: build
  tags:
    - web
  script:
    - mkdocs --verbose build

rsync_deploy:
  stage: deploy
  tags:
    - web
  script:
    - rsync -avr --progress --delete public/ /var/lib/www/docs.hpc.sjtu.edu.cn/
